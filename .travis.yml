language: node_js

cache:
  directories:
    - node_modules

notifications:
  email: false

node_js:
  - 'node'

script:
  - npm run test

after_success:
  - npm i -g semantic-release
  - semantic-release pre
  - npm run build:lib
  - cd lib
  - npm publish
  - cd ..
  - semantic-release post

branches:
  only:
    - master
